document.addEventListener("DOMContentLoaded", () => {
  const searchButton = document.getElementById("exploreSearchButton");
  const searchInput = document.getElementById("exploreSearchInput");
  const categoryFilter = document.getElementById("exploreCategoryFilter");
  const sortSelect = document.getElementById("exploreSort");

  if (!searchButton || !searchInput || !categoryFilter || !sortSelect) {
    return;
  }

  // Search functionality will be added in its own JavaScript file later.
  // Filter functionality will be added in its own JavaScript file later.
  // Sort functionality will be added in its own JavaScript file later.
});
